export class Book {
  constructor(title, author, coverPhotoURL, readingLevel) {
    this.title = title;
    this.author = author;
    this.coverPhotoURL = coverPhotoURL;
    this.readingLevel = readingLevel;
  }
}
